@model IEnumerable<NAMIS.Models.locals>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}



<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.state_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LGAOrigin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.state_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LGAOrigin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.local_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.local_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.local_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
