@model IEnumerable<NAMIS.Models.department>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}




<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Department)
            </td>
            <td>
                <a asp-action="Edit" asp-controller="departments" class="btn btn-danger btn-rounded btn-sm my-0" asp-route-id="@item.DeptID">Edit</a> |
                <a asp-action="Details" asp-controller="departments" class="btn btn-danger btn-rounded btn-sm my-0" asp-route-id="@item.DeptID">Details</a> |
                <a asp-action="Delete" asp-controller="departments" class="btn btn-danger btn-rounded btn-sm my-0" asp-route-id="@item.DeptID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
